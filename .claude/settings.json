{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,subprocess; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); f and subprocess.run(['npx','prettier','--write',f])\" 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "npm run type-check && npm run lint",
            "timeout": 120000
          }
        ]
      }
    ]
  }
}
